// condicionales
/**xxx
 * operadores de comparacion
 * (==) comparamos valores sin importar el tipo de dato
 * (===) comparamos valores respetando el tipo de dato
 * (>) Mayor que
 * (<) Menor que
 * (>=) Mayor igual
 * (<=) Menor igual
 * (!=) Diferente no respeta tipos 
 * (!==) Diferente respeta tipos
 */

/**
 * Operdadores logico
 * (||) - O
 * (&&) - Y
 */

// declaramos la variable edad
const age = prompt("ingrese su edad");
const country = prompt("ingrese su pais").toLowerCase();

// Condicional if/else "16"

if (age >= "16" && country !== "alemania") {
    alert("Se habilito el catalogo de peliculas violentas")
} else {
    alert("No tienes permiso de ver este catalogo")
}

// 
/**
 * NOTA: 
 * Mayusculas (x)
 * Tildes ()
 * Vacios ()
 * nulos ()
 * convertir tipos de datos ()
 */